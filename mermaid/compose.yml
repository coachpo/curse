services:
  # Mermaid Live Editor - diagram editor UI
  mermaid-live-editor:
    image: ghcr.io/mermaid-js/mermaid-live-editor:latest
    container_name: mermaid-live-editor
    platform: linux/arm64
    restart: unless-stopped
    ports:
      - "${MERMAID_PORT:-8083}:8080"
    networks:
      - mermaid-network

networks:
  mermaid-network:
    driver: bridge
