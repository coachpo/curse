:80 {
    # Serve cached/static resources from host path at /static-cache/*
    handle_path /static-cache/* {
        root * /srv/openlist-cache
        file_server browse
    }

    # Proxy all other requests to the OpenList app
    reverse_proxy * openlist:5244
}
