name: clay

services:
  clay:
    image: ghcr.io/coachpo/clay:${CLAY_VERSION:-latest}
    restart: unless-stopped
    ports:
      - "${CLAY_PORT:-8089}:8000"
    env_file:
      - ./.env
    networks:
      - clay-network

networks:
  clay-network:
    driver: bridge
