name: clay-c

services:
  clay:
    image: ghcr.io/coachpo/clay:latest
    restart: unless-stopped
    ports:
      - "${CLAY_C_PORT:-8091}:8000"
    env_file:
      - ./.env
    networks:
      - clay-network

networks:
  clay-network:
    driver: bridge
