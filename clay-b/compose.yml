name: clay-b

services:
  clay:
    image: ghcr.io/coachpo/clay:latest
    restart: unless-stopped
    ports:
      - "${CLAY_B_PORT:-8090}:8000"
    env_file:
      - ./.env
    networks:
      - clay-network

networks:
  clay-network:
    driver: bridge
